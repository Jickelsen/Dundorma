APP_ENV="local"
APP_DEBUG="false'
APP_KEY="${APP_KEY}"

DB_HOST="${DB_ADDR}"
DB_DATABASE="${DB_NAME}"
DB_USERNAME="${DB_USER}"
DB_PASSWORD="${DB_PASS}"

RTDB_HOST="${RTDB_ADDR}"
RTDB_DATABASE="${RTDB_NAME}"
RTDB_USERNAME="${RTDB_USER}"
RTDB_PASSWORD="${RTDB_PASS}"

CACHE_DRIVER="file"
SESSION_DRIVER="file"
QUEUE_DRIVER="sync"
BROADCAST_DRIVER=redis

REDIS_HOST="${REDIS_ADDR}"
REDIS_PASSWORD="${REDIS_PASS}"
REDIS_PORT="${REDIS_PORT}"
